# Same as frontend/.env — talks to hosted backend (no local port 5000 needed)
VITE_API_URL=https://imototo-sc43.onrender.com

# Secret path is in src/config.js (ADMIN_SECRET_PATH) — change there if needed
